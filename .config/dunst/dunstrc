
[global]
	# Display # 
	
	monitor = 0
	follow = keyboard
	width = (300, 600)
	height = (50, 200)
	notification_limit = 20
	origin = top-right
	offset = (0, 0) # Margin of 10 pixels from the right and 300 from the top
	padding = 4
	horizontal_padding = 4
	scale = 0
	frame_width = 2

	# Text #
	font = 0xProto Nerd Font Mono 12 # default = Monospace 8
	format = "<b>%s</b>\n%b"

	# Icons #
	icon_path = /usr/share/icons/
	icon_theme = Cosmic, breeze-dark, Pop, Adawaita, hicolor, locolor, default
	enable_recursive_icon_lookup = true
